// Supabase runs server-side only (API server).
// All DB operations go through /api/* endpoints — never directly from the browser.
// This file is intentionally empty; import from @/lib/api instead.
export {}
