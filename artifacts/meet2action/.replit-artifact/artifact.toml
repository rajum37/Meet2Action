kind = "web"
previewPath = "/"
title = "Meet2Action"
version = "1.0.0"
id = "artifacts/meet2action"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/meet2action run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/meet2action", "run", "build" ]
publicDir = "artifacts/meet2action/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
